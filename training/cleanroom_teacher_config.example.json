{
  "name": "authorized_teacher_A",
  "command": [
    "C:/path/to/authorized_renderer_shim.exe",
    "--model",
    "{model}",
    "--midi",
    "{midi}",
    "--controls",
    "{control_json}",
    "--output",
    "{output}",
    "--seed",
    "{seed}"
  ],
  "model": "D:/models/teacher_A.model",
  "timeout_seconds": 180,
  "fingerprint_model": false,
  "rights": {
    "authorized_runtime_use": false,
    "output_training_allowed": false,
    "learned_weight_release_allowed": false,
    "commercial_use_allowed": false,
    "notes": "Fail-closed example. Change flags only after you have verified the applicable license/permission."
  }
}
